name: CI

on:
  workflow_call:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cs-fixer:
    uses: ./.github/workflows/cs-fixer.yml

  phpstan:
    uses: ./.github/workflows/phpstan.yml

  rector:
    uses: ./.github/workflows/rector.yml

  compat:
    uses: ./.github/workflows/compat.yml

  tests:
    needs: [cs-fixer, phpstan, rector, compat]
    uses: ./.github/workflows/tests.yml

  coverage:
    needs: [cs-fixer, phpstan, rector, compat]
    uses: ./.github/workflows/coverage.yml

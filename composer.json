{
    "name": "flowd/kochmodus-wordpress-plugin",
    "description": "Kochmodus cooking mode widget for WordPress",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": "^7.4|^8.0"
    },
    "require-dev": {
        "brain/monkey": "^2.6",
        "friendsofphp/php-cs-fixer": "^3.0",
        "fsylum/rector-wordpress": "^1.0",
        "mockery/mockery": "^1.6",
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^13.1.7",
        "rector/rector": "^2.4",
        "rregeer/phpunit-coverage-check": "^0.3.1",
        "szepeviktor/phpstan-wordpress": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "Kochmodus\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Kochmodus\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:unit": "phpunit --testsuite=unit",
        "test:coverage": [
            "@putenv XDEBUG_MODE=coverage",
            "phpunit --coverage-clover=coverage.xml --coverage-html=coverage/html --coverage-text",
            "coverage-check coverage.xml 78"
        ],
        "analyse": "phpstan analyse",
        "cs": "php-cs-fixer fix --diff --dry-run",
        "cs:fix": "php-cs-fixer fix --diff",
        "rector": "rector process --dry-run",
        "rector:fix": "rector process"
    },
    "config": {
        "sort-packages": true
    }
}
